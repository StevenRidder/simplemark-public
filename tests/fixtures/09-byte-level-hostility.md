# Byte-level hostility

This paragraph ends with two trailing spaces  
and the hard break above must survive.

The next three lines use CRLF endings:
first CRLF line
second CRLF line with a trailing tab	

Indented with a hard tab, not spaces:

	tab-indented code block
		twice tab-indented

| ragged	| table		|
|---|---|
| a		| b |

A line with trailing whitespace before a blank line:   
   
Mixed	tabs	and spaces   inside a sentence.

The file ends on the next line with no final newline.
no trailing newline here